

dependencies {
  compile project(':plugins')
  compile project(':csv-plugins')
  compile project(':filesystem-plugins')
  compile project(':sql-plugins')
  if(useXdb()) {
    compile project(':xdb-plugins')
  }
}